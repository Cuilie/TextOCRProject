const serverless = require('./serverless-http');

const handler = serverless((req, res) => {
    res.end('test');
});

async function run() {
    while (true) {
        await handler({
            path: '/',
            method: 'POST',
            body: 'hello'
        });
    }
}

run().catch(console.error);
